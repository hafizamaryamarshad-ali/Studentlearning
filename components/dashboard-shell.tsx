import Link from "next/link";
import { BarChart3, BookOpen, GraduationCap, Home, Menu, Settings, ShieldCheck } from "lucide-react";
import { ReactNode } from "react";

export function DashboardShell({ title, eyebrow, children, admin = false }: { title: string; eyebrow: string; children: ReactNode; admin?: boolean }) {
  const links = admin ? [[BarChart3,"Overview"],[BookOpen,"Courses"],[ShieldCheck,"Users"],[Settings,"Settings"]] : [[Home,"Overview"],[BookOpen,"My learning"],[BarChart3,"Progress"],[Settings,"Settings"]];
  return <div className="min-h-screen bg-slate-100 text-slate-950"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-17 max-w-[1440px] items-center justify-between px-5 sm:px-8"><Link href="/" className="flex items-center gap-2 font-black"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><GraduationCap className="h-5 w-5" /></span>SkillSpring</Link><Link href="/" className="text-sm font-bold text-blue-700">Back to home</Link></div></header><div className="mx-auto grid max-w-[1440px] md:grid-cols-[240px_1fr]"><aside className="hidden min-h-[calc(100vh-68px)] border-r border-slate-200 bg-white p-5 md:block"><p className="px-3 text-xs font-extrabold uppercase tracking-widest text-slate-400">{admin ? "Admin workspace" : "Student space"}</p><nav className="mt-4 space-y-1">{links.map(([Icon,label],i) => { const I=Icon as typeof Menu; return <span key={label as string} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold ${i===0?"bg-blue-50 text-blue-700":"text-slate-600"}`}><I className="h-5 w-5" />{label as string}</span>})}</nav></aside><main className="min-w-0 p-5 sm:p-8 lg:p-10"><div className="mx-auto max-w-5xl"><p className="text-sm font-extrabold uppercase tracking-[.16em] text-blue-600">{eyebrow}</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">{title}</h1><div className="mt-8">{children}</div></div></main></div></div>;
}
